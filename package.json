{
  "name": "robobala-interface-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
